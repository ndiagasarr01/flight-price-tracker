name: Vérification hebdomadaire du prix du vol

on:
  schedule:
    # Chaque lundi 10h00 UTC = 5h00/6h00 heure de Québec (selon heure d'été/hiver)
    # 12 combinaisons de dates par exécution × ~4 semaines/mois ≈ 48 requêtes SerpApi/mois
    - cron: "0 10 * * 1"
  workflow_dispatch: {}   # permet de le lancer manuellement depuis l'onglet Actions

permissions:
  contents: write

jobs:
  check-price:
    runs-on: ubuntu-latest
    steps:
      - name: Cloner le dépôt
        uses: actions/checkout@v4

      - name: Installer Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Installer les dépendances
        run: pip install -r requirements.txt

      - name: Lancer la vérification et envoyer le courriel
        env:
          SERPAPI_KEY: ${{ secrets.SERPAPI_KEY }}
          EMAIL_ADDRESS: ${{ secrets.EMAIL_ADDRESS }}
          EMAIL_APP_PASSWORD: ${{ secrets.EMAIL_APP_PASSWORD }}
          EMAIL_TO: ${{ secrets.EMAIL_TO }}
          DEPARTURE_ID: "YUL"
          ARRIVAL_ID: "DSS"
          OUTBOUND_DATES: "2026-12-19,2026-12-20,2026-12-21"
          RETURN_DATES: "2027-01-08,2027-01-09,2027-01-10,2027-01-11"
        run: python flight_price_tracker.py

      - name: Sauvegarder l'historique des prix dans le dépôt
        run: |
          git config user.name "flight-price-bot"
          git config user.email "actions@github.com"
          git add price_history.json
          git diff --quiet --cached || git commit -m "Mise à jour du prix du $(date -u +%Y-%m-%d)"
          git push
